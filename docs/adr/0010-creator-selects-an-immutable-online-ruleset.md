# Let the creator select an immutable Online Ruleset

The creator selects Classic or Greed when creating a Private Room, after which the Online Ruleset is immutable; the invited Guest Player sees its mode, fixed configuration, and versions before accepting the second seat. Voting, random mode selection, and post-creation edits were rejected because stable invitation semantics and a small Room Setup state machine are more valuable for the MVP.
