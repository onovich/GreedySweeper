# Randomize the Opening Player on the server

After both Guest Players accept Room Setup, the Match Authority randomly selects the Opening Player and records the assignment in canonical match metadata and the replay. Giving the creator a fixed first move was rejected because room ownership must not confer a gameplay advantage; rematch-based alternation remains outside the MVP.
